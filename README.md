# emotion_detection
This project is a Facial Emotion Recognition System built using Convolutional Neural Networks (CNN) and OpenCV. It can train a deep learning model on facial emotion datasets and detect emotions in real time through a webcam feed.
